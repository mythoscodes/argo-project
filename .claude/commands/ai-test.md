AI 퀴즈 생성 품질을 테스트합니다.

1. 현재 `AI_MODEL` 환경변수 확인
2. 지정된 과정(Spring/Python/보안/네트워크/AI개발)으로 퀴즈 5문항 생성
3. 각 문항에 대해 평가:
   - 코드 스니펫이 실행 가능한 실제 코드인지
   - 한국어 지문이 자연스러운지
   - misconception 태그가 교육적으로 의미 있는지
   - JSON 파싱이 Zod 스키마를 통과하는지
4. 품질 등급 매기기 (★★★★★ ~ ★)
5. 등급이 ★★★ 이하이면 옵션 B(Claude Sonnet) 전환 권장

인자로 과정명을 받습니다. 예: `/ai-test python` 또는 `/ai-test spring`
